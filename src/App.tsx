// The delivered app is a single self-contained HTML file (index.html):
// inline CSS + vanilla JS, no framework, per the brief. This stub exists
// only so the React scaffold compiles — it is never mounted.
export default function App() {
  return null;
}
